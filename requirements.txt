# Librerie Essenziali (Offline)
customtkinter==5.2.0
pandas>=2.0.0
openpyxl>=3.1.0

# Librerie per Report e Grafici (Offline)
matplotlib>=3.7.0
seaborn>=0.12.0

# Librerie per Documenti PDF (Offline)
reportlab>=4.0.0

# Librerie per Date e Orari (Offline)
python-dateutil>=2.8.0
pytz>=2023.3

# Librerie per Ricerca (Offline)
fuzzywuzzy>=0.18.0
python-levenshtein>=0.21.0

# Librerie per Immagini (Offline)
Pillow>=10.0.0
qrcode>=7.4.0

# Librerie per Sicurezza (Offline)
cryptography>=41.0.0
bcrypt>=4.0.0

# Librerie per Icone e Desktop (Offline)
pystray>=0.19.0
pywin32>=306

# Dipendenze per la sincronizzazione dispositivi mobili
pyserial>=3.5
pybluez>=0.23

# Librerie per Calcoli Finanziari (Offline)
# decimal è built-in in Python

# Librerie per Sistema Audio (Offline)
pygame>=2.6.0
scipy>=1.11.0
numpy>=1.24.0

# Librerie per Performance e Monitoraggio (Offline)
psutil>=5.9.0

# Librerie per Creazione Eseguibile (Sviluppo)
pyinstaller>=6.0.0
